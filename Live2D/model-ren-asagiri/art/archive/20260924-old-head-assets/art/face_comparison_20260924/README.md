# 最新顔パーツのPNG合成比較 — 2026-09-24

Boss指定の `art/eye_adjust_v007/parts/` 直下の14個別PNGだけで顔を合成。`facial_feature.png` が配置見本で、同フォルダーの `facial_feature.psd` は座標・反転の照合にのみ使用。PSD内の画像を合成素材に流用していない。旧版・backup・archive・他部位の素材は使用していない。

## 確認画像

- `face_sample.png`：850×1134、最新個別PNGから再合成した透明背景の顔。
- `face_sample_gray.png`：上記を灰青背景に重ねた確認画像。
- `comparison.jpg`：左が指定配置見本、右が再合成。共通倍率。
- `detail_comparison.jpg`：配置見本と再合成の目鼻口の拡大。
- `original_features_comparison.jpg`：同フォルダーの原画、配置見本、再合成の顔部分比較。原画は比較専用で、合成素材にはしていない。

## 配置と差分

最新face_underfillを(121,135)、閉口を(380,854)へ配置。原点は850×1134の配置見本の左上。個別ファイルのサイズを保ち、PSDの該当層と画素を照合して平行移動・必要な左右反転のみで組み立てた。14パーツ全てで拡縮は選択されていない。

青目側の白目と下まぶたには反転を適用した。右下まぶたの最新PNGは58×15で、見本PSDの該当表示132×41と異なる。右下まぶたは見本内で対応する中央〜鼻側の下縁に合わせた暫定位置(266,696)。upper_crease左右、青目白目もPSD内の形と完全一致しないため、現在のPNG形状を保ち該当領域で照合した位置で配置。これらの位置は微調整候補として扱い、ピクセル完全一致とは報告しない。その他の眉・虹彩・まつ毛・口・茶目白目・青目下まぶたはPS​​D表示パーツとの画素照合がほぼ一致。

今回の顔下地の陰影は実PNGから反映されている。配置見本を完成画像として流用していない。部品の違いに伴い、茶目下縁と眼窩の線などは見本と少し異なる。差を消すために古い部品や切り出した見本を混ぜる処理はしていない。

髪・耳・首の個別パーツは指定フォルダー内にないため、合成は顔のみ。原画比較の位置は鼻テクスチャを用いた平行移動で推定し、合成パーツの配置には使用していない。

## 検証と保存

`assembly.json` に14部品のファイル名・配置・反転・サイズ・照合値・入力ハッシュを記録。全入力のハッシュ不変を確認。使用PNGの控えはsource_snapshot。PSD画像の検査用書き出しreference_layer_*は照合専用で、完成画像には使用していない。

PNG比較を目視確認済み。新規PSDの作成、Cubism操作、原画の塗り修正、顔素材フォルダーの移動は行っていない。PNG段階で品質を整えてからPSD/Cubismへ進むBossの方針を維持。親Agentのみ・子Agent0、設定変更なし、commit/pushなし。
