# Ren 初期パーツ・PSD・Cubism配置 検査記録

確認日：2026-09-21（JST）。対象は朝霧レンの前後原画と補助素材のみ。

## 結果

**パーツ分割・位置合わせ・PSD出力・Cubismへの読み込みとcmo3保存は完了。リグと作画の最終仕上げは未完。**

| 検査 | 正面 | 背面 |
|---|---:|---:|
| キャンバス | 4000×6000 | 4000×6000 |
| PSDレイヤー | 51（39表示・12非表示） | 22（全表示） |
| 原画と通常レイヤー再合成の透明度差 | 0 pixel | 0 pixel |
| 原画の欠損／外側への追加 | 0／0 pixel | 0／0 pixel |
| 不透明・半透明領域のRGB差 | 0 pixel | 0 pixel |
| PSD各層RGBA・名前・位置・順序・表示状態 | 一致 | 一致 |
| 独立ライブラリ再描画の透明度差 | 0 pixel | 0 pixel |
| 独立再描画の表示色差が2/255を超えるpixel | 0 | 0 |
| Cubism読み込み・保存・閉じて再読込 | 確認済み | 確認済み |

元ファイル11点はSHA-256で変更なしを確認。[source_hashes.json](source_hashes.json)、[validation.json](validation.json)が根拠。半透明の外周を保持し、全体を縮める境界消去処理は行っていない。

PSDはRGB 8bit。ag-psdで書き出した後、各層の画像と配置を読み戻して照合した。[正面](front/psd_verification.json)／[背面](back/psd_verification.json)。別実装のpsd-tools 1.12.1でレイヤーを強制合成し、薄い背景・灰色背景の双方で確認。表示色の最大差は両面とも約0.984/255で、アルファ差はゼロ。[正面](front/psd_tools_verification.json)／[背面](back/psd_tools_verification.json)。

PSDの埋め込み合成プレビューをag-psdで読み戻すと、white-matte変換による半透明境界RGB差がある（正面32,484 pixel、背面34,205 pixel）。実レイヤーRGBAの一致と区別し、診断を上記JSONに保存している。独立再描画では完全透明領域のRGB値も一致しないが、透明度と背景上の表示を別に検証した。

## Cubism実機確認

- Live2D Cubism Editor **5.3.04 FREE版**。PRO体験版の開始は行っていない。
- 最終PSDを前面・背面それぞれのモデルへ反映し、`model/parts_v001/`へ保存。前面保存12:47、背面保存12:50（JST）。
- 12:52〜12:54頃に両モデルを一度閉じ、保存済みcmo3を読み直した。アホ毛から靴まで表示され、原画の通常表情と分割レイヤー一覧を確認。正面の開口・目候補は非表示を維持。
- 最終状態は両モデルを開いたまま、正面タブを表示。再インポート前の未使用原画情報はモデル内に保持した。
- 画面上の読み込み確認は視認検査であり、cmo3内部の全画素・全属性を独自解析して照合したものではない。

最終PSDとcmo3のファイル容量・保存時刻・SHA-256は[delivery_manifest.json](delivery_manifest.json)に記録。

## 作画・動作の確認範囲

通常合成、顔の拡大、開口候補、提供目素材の配置候補、髪を隠した頭部下地を視認。確認画像は[README](README.md)から参照可能。

髪下の額・耳付け根などの継ぎ目、目素材の画風・位置の微調整、動作で露出する毛束根元や関節の描き足しはBossの仕上げ対象。指別分割、下まつ毛・上下唇の細分化、下歯素材は未作成。提供された目素材6点は元サイズのファイルを保管し、配置用コピーをPSDへ取り込んだ。

初期ArtMeshの配置までを完了としている。変形用メッシュの調整、デフォーマ、パラメータのキー、まばたき、口パク、物理演算、テクスチャアトラス、moc3、配信用設定、前後の連続回転は未実装・未検証。

## 再現性と担当

- `tools/build_all.ps1 -Verify`を実行し、素材再生成・検査・PSD作成・独立検査まで終了コード0。生成工程の観測値は約21秒。UI工程を含む全体所要時間は厳密には計測していない。
- 既存リサ用PSD処理を再利用。Pillow、NumPy、作業フォルダ内OpenCV、ag-psd、pngjs、psd-toolsを使用。Cubismの読み込み・保存はComputer Useで操作。
- 親Astraが分割実装・統合・GUI・最終判定を担当（親の推論強度は未計測）。子Agent1名、Terra／Medium指定、forkなしで既存PSD処理の独立監査と検証を担当。GUIは親のみ操作。使用トークン・料金は取得していない。
- 途中の検査で見つかった髪下地の混入と半透明境界の重複を修正し、最終版で原画の見えるRGB・アルファ一致を確認した。最終検査通過後の追加の作画変更はない。
- Git：`main`、作業開始時HEAD `5944b0bd0ae9e2f40573acac1281520be3223d29`。README更新、生成素材・モデル追加。stage／commit／pushは実行していない。

仕様確認に使用した公式資料：[PSDのインポート](https://docs.live2d.com/en/cubism-editor-manual/psd-import/)、[PSD作成時の注意点](https://docs.live2d.com/en/cubism-editor-manual/precautions-for-psd-data/)。今回の成功判定は上記ファイル検査と実機操作に基づく。
