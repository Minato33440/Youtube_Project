# 襟・裏地の再配置 v002

2026-09-21、Bossの20:28〜20:29色調整版3枚を添付サンプルに合わせてPSD化。

- `Ren_collar_assembly_v002.psd`：567×454、RGB 8bit、3レイヤー。
- `collar_comparison.jpg`：左がサンプル、右が再配置。
- `collar_assembled.png`：透過合成。
- `parts/`：原本のコピー。画素・サイズ・アルファは変更なし。

下から `shirt_inside`、`collar_R`、`collar_L`。位置は(1,0)、(9,22)、(248,19)。素材内の座標であり、全身4000×6000上の座標ではない。柄をサンプルに照合して配置し、目視確認済み。サンプルは567×453だが、下端を切らないよう高さ454を維持。

原本PNG、全身PSD、全身manifest、cmo3、口素材は更新していない。今回の範囲は3枚の襟・裏地のローカル再配置。

裏地の画面右上に細い白いはみ出しがある。元PNGのまま保持しているため、必要に応じて仕上げる。首・顔を重ねた際の隠れ方も確認する。

検証：ag-psd書込・読み戻しで3層のRGBA、名前、座標、再合成一致PASS。独立psd-tools読込もPASS（合成丸めにより背景上のチャンネル差最大約1.5/255）。サンプルの不透明内部との差平均約0.92/255、95パーセンタイル2/255。スクリーンショットとの表示比較と原本画素一致は別検査。各検査JSONに詳細。

`build.py` は同フォルダの出力を再生成するため、手修正後には再実行しない。親Agentのみ、子Agent 0。モデル・推論強度・消費量の有効値は未計測。Cubism動作検査は今回の範囲外。
