# Youtube_Project — Astra制作運用

## 方針（2026-09-10、Bossとの合意）

品質を保ちながら推論コストも考慮する。最安モデルへの固定やAgent数の最大化を目的にしない。
政治・経済動画のコード処理主体の工程は `Politics_Economics/CODE_VIDEO_PRODUCTION_WORKFLOW.md`、Computer-useによるFilmora主体の工程は `Politics_Economics/VIDEO_PRODUCTION_WORKFLOW.md` を参照する。
企画・原稿・素材選定は `docs/channel_editorial_direction.md` を参照し、合意した目的と未決定のチャンネル構成案を区別する。

## 制作と品質

- 構成確認用MP4はPython/FFmpegによる切り抜き・結合・字幕/画像配置・音声調整を優先する。
- 構成のすり合わせ後、細かな演出・手動調整はFilmoraで行う。各素材・字幕・配置時刻を保持し、編集可能なFilmoraプロジェクトも成果物に含める。統合MP4だけではレイヤーを再編集できない。
- 発言の前後関係、否定、成立条件、反対意見を落とさない。事実、話者の主張、Bossの意図、編集者の考察、未確認事項を区別する。
- 機械的な検査はスクリプトで行う。尺、音声欠落、音ズレ、字幕表示、カット境界を確認し、技術検査合格と内容・視聴品質の確認を区別する。
- 手順をコード化しても映像・音声の確認を省略しない。Agent同士の一致を一次資料の代わりにしない。

## 推論強度

- Astraの通常制作はMediumを開始点とする。既定の結合、素材配置、字幕修正、既存コードの小修正、通常のFilmora操作に適用する。
- 文脈が曖昧な切り抜き、政策・数値の整合性、原稿の論理、複雑な不具合はHighを検討する。Highでも解決しない対立や全体整合性の難題に限りExtra Highを検討する。
- Mediumで全工程の品質が実証済みとは扱わない。2026-09-09の制作ログにはLow/Medium/High/Extra Highが混在していた。
- AGENTS.mdやconfig.tomlを書き換えただけで、実行中タスクの推論強度が切り替わったとは報告しない。利用可能な制御で指定し、実効値を確認できない場合は推奨値と区別する。

## 子Agentの配分

- Bossは必要に応じた子Agent利用とモデル・人数・引継ぎ履歴の調整を許可している。具体的で独立した作業があり、親も並行して有用な作業を進められる場合に限って委任する。
- 小さな修正や直列作業は子Agent 0。通常は0〜1、独立した実装と検証などに実益がある場合は2。人数は毎回判断し、人数を埋めるために仕事を分けない。
- GPT-5.6 Luna：範囲が明確な抽出、一覧化、書式変換、メタデータ照合。曖昧な発言の解釈や最終編集判断を単独で任せない。
- GPT-5.6 Terra：通常のスクリプト実装、字幕・タイムライン処理、技術検証。標準的な作業担当。
- GPT-5.6 Sol：複数ファイルにまたがる難しい修正、複雑な根拠照合、独立した内容レビュー。
- Astra：Bossとの構成合意、意味・論点の統合、例外判断、重要な根拠への再確認、最終品質判断。
- 子Agentの推論強度も明示的に選ぶ。定型処理はLow/Medium、通常実装はMedium、複雑なレビューはHighを開始点とし、必要に応じて調整する。モデル固定の連鎖や同じ失敗の反復を避け、原因に応じて資料補充・モデル変更・親への引取りを選ぶ。
- FilmoraのGUIは同時に1担当だけが操作する。同じファイルを複数Agentが同時編集しない。

## 引継ぎコンテキスト

- `spawn_agent`の`fork_turns`は省略せず、原則`"none"`とする。直近の対話が必要なら必要最小限の正の整数文字列を指定する。
- `"all"`は全会話への依存が強く、短い引継ぎで重要な合意や反証を失う場合だけ使用し、理由を記録する。現ランタイムではallが親モデル/推論を継承し、モデル上書きと併用できない点に注意する。
- 履歴を減らす場合も、目的、担当範囲、関連ファイルの絶対パス、必要な一次資料・タイムコード、採用/不採用の判断と理由、制約、未確認事項、合格条件、編集権限を渡す。
- 出力は成果物パス、変更要点、根拠、検証結果、未解決点に絞る。長い会話・ログを丸ごと親へ戻さない。結果に疑義があれば親が原資料を開く。

## コスト評価と設定範囲

- `test_report.md`等に工程ごとのモデル、推論強度（観測値/指定値を区別）、子Agent数、fork範囲と理由、所要時間、手戻り、欠陥、検証範囲を簡潔に記録する。
- 使用量が取得できる場合だけ記録する。API料金からCodex契約枠の消費を断定せず、並列化・短い履歴・下位モデルが必ず節約になるとも断定しない。
- 検査・再作業・Bossの確認負担を含めて品質と効率を評価する。受入品質を下げてコストを良く見せない。
- このプロジェクトの既定値は `.codex/config.toml` に置く。グローバル設定や他プロジェクトのSkillは、当プロジェクトの都合だけで書き換えない。
