# リサ N01 — サイズ・動作調整サンプル v1

2026-09-16。Boss指定の調整案を反映した、約16秒のナレーション部分のみの比較候補。既存のMP4、Filmoraプロジェクト、cmo3、v1〜v4および全編用データは変更していない。

## 出力と変更

- MP4: `../../../Politics_Economics/2026-09-09_fiscal_policy/exports/Risa_Narration_N01_SizeMotion_Adjusted_v1.mp4`
- 1920×1080 / 30fps / 479フレーム / 15.966667秒。元試作の21.433333〜37.4秒に相当するN01。
- キャラクターの縦横倍率1.5。元の腰までの構図から胸元までに切り替え、表示領域576×450、左上(1180,70)、下端520に配置。説明カードの上端545まで25px確保。
- 口の開閉カーブを1.2倍、首の傾きカーブを2倍、負の口形状値（すぼめ側）を0.55倍。正の口形状値は維持。無音・閉唇の0値、各発音時刻、首の強調時刻・回数・動作時間、まばたき・呼吸は維持した。
- 今回は既存リグの範囲内で演技データを変更。原画・メッシュの編集は不要だった。cmo3自体は更新していない。
- 新しい `performance_curve.csv` と `performance.motion3.json`、調整値 `adjustment_profile.json` を保存。`model/Risa_adjusted.model3.json` は既存v4のmoc3・テクスチャを相対参照するため、このフォルダーだけでは独立モデルパッケージにならない。

## 確認結果

- 既存音声N01の整列済みカーブを使用。以前のVoicd-Sample用v4時刻は流用していない。
- 全479行で指定倍率と、発音・まばたき・呼吸の保持を検査。開口0〜0.958432、口形状-0.549979〜0.899586、首-8.382048〜7.588296（パラメーター値）。
- Cubism SDKで新しいmotion3を実際に再生して描画。CSVに対する全6軸の最大誤差は0.001247未満（許容0.003）。自動まばたき・自動呼吸は無効。1152×1536で描画し、透過フレームを縮小合成。
- 全479フレームのデコード成功。映像・音声の開始時刻0、尺の差は1フレーム未満。
- 入力WAVのSHA-256一致。MP4のAAC音声を16kHzモノラルで照合したゼロオフセット相関0.999982、相関ピークのずれ0ms。
- 開口最大、すぼめ最大、首左右最大、まばたき、始終端を含む9フレームを抜き出し、出力モデルと透過原画像の対応およびモデル領域内の時間差分を検査。背景や字幕の変化だけで動きありとは判定していない。
- 全フレームのアルファ境界で頭上・左右の切れがないことを確認。胸元での下端切り抜きは意図した構図。
- `layout_preview.jpg` と `face_neck_review.jpg` を目視し、説明カード・字幕への重なり、確認姿勢での襟元の目立つ隙間、口の分離は認めなかった。これは全姿勢の安全性の証明ではない。
- Bossによる音声付き通し視聴と自然さの受入は未実施。今回の親による目視確認も静止フレームであり、音声付き通し試聴とは区別する。

機械検査: `verification.json` / `sdk_render_verification.json`。入力・出力ハッシュと描画所要時間: `manifest.json`。

## 再生成

プロジェクトルートから以下を実行する。既存モデル・背景・音声および `runtime_venv` が必要。

```powershell
output/live2d/runtime_venv/Scripts/python.exe output/live2d/narration_adjustment_v1/build_sample.py
output/live2d/runtime_venv/Scripts/python.exe output/live2d/narration_adjustment_v1/verify_sample.py
```

同名の今回サンプルと中間データを再生成するコマンド。旧版サンプルは上書きしない。別の調整候補を作る場合は出力先と版名も変更する。

作業記録: 親Astraが実装・検証・フレーム確認を担当。実効推論強度は未確認、子Agent0、forkなし。描画・合成42.95秒、全作業時間・契約使用量は未計測。パッケージ追加なし、処理エラーによる手戻りなし。Filmora側への反映は今回の範囲外。
